# Production deployment files

- `Dockerfile` — Next.js standalone production image
- `docker-compose.prod.yml` — app + Postgres + Caddy
- `deploy/Caddyfile.example` — annotated reverse proxy template
- `.env.production.example` — production env template

See [DEPLOY_HETZNER.md](../docs/DEPLOY_HETZNER.md) for full instructions.
