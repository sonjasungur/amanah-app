name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

env:
  NEXT_TELEMETRY_DISABLED: "1"
  AMANAH_AI_PROVIDER: rules
  AI_PROVIDER: rules
  AMANAH_AI_ENABLED: "false"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint, unit tests, and build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npm run lint

      - name: Unit tests
        run: npm test -- --run

      - name: Production build
        env:
          NEXT_PUBLIC_AUTH_MODE: local
          NEXT_PUBLIC_STORAGE_MODE: local
        run: npm run build

  e2e:
    name: Playwright E2E – desktop and mobile
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Production build
        env:
          NEXT_PUBLIC_AUTH_MODE: local
          NEXT_PUBLIC_STORAGE_MODE: local
        run: npm run build

      - name: Playwright E2E
        env:
          CI: "true"
          PLAYWRIGHT_PORT: "3099"
          NEXT_PUBLIC_APP_URL: "http://127.0.0.1:3099"
        run: npm run test:e2e

      - name: Upload Playwright failure artifacts
        if: failure() || cancelled()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-failure-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7

  e2e-failure-paths:
    name: Playwright E2E – phase2 failure paths (mocked API)
    needs: quality
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Production build (API mode – no real credentials needed)
        env:
          NEXT_PUBLIC_AUTH_MODE: api
          NEXT_PUBLIC_STORAGE_MODE: api
          NEXT_PUBLIC_AMANAH_AI_ENABLED: "true"
        run: npm run build

      - name: Playwright failure paths (all endpoints mocked)
        env:
          CI: "true"
          PLAYWRIGHT_PORT: "3099"
          NEXT_PUBLIC_STORAGE_MODE: api
          PHASE2_API_TESTS: "true"
        run: npx playwright test e2e/phase2-failure-paths.spec.ts --project=chromium-desktop

      - name: Upload failure artifacts
        if: failure() || cancelled()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-failure-paths-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7
